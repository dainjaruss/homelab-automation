# PostgreSQL Configuration
POSTGRES_USER=n8n
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=n8n

# n8n Configuration
N8N_PORT=5678
N8N_PROTOCOL=http
N8N_HOST=192.168.1.142
N8N_EDITOR_BASE_URL=http://192.168.1.142:5678
WEBHOOK_URL=http://192.168.1.142:5678

# n8n Authentication (Basic Auth)
# Change these to secure credentials!
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Timezone
TIMEZONE=America/New_York

# ============================================
# INSTRUCTIONS:
# ============================================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and change:
#    - POSTGRES_PASSWORD to a strong password
#    - N8N_BASIC_AUTH_PASSWORD to a strong password
#    - Optionally change N8N_BASIC_AUTH_USER
#
# 3. Keep .env file secure and never commit it to Git!
#
# 4. If you want external access, consider setting up:
#    - Reverse proxy (Nginx, Traefik)
#    - SSL certificates (Let's Encrypt)
#    - Change N8N_PROTOCOL to https
